{
  "name": "@texttrends/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Node benchmark/portability harness over @texttrends/core (a built distributable with real subcommands comes later).",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@texttrends/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.12.0",
    "typescript": "^5.9.0"
  }
}
