// texttrends CLI — Node adapter over @texttrends/core.
//
// Purpose (per the portable-core decision, synthesis §8.10): prove the core's
// portability from day one, run conformance fixtures and benchmarks headlessly,
// and offer batch analysis with machine-readable (JSON/CSV) output. Grows with
// the core; first real subcommands land with the tokenizer/index.

process.stdout.write('texttrends cli — engine not yet implemented\n');
process.exitCode = 1;
