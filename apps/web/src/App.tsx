export function App() {
  return (
    <main style={{ padding: 'var(--space-4)', maxWidth: '48rem' }}>
      <h1 style={{ fontSize: 'var(--text-lg)', fontWeight: 600, margin: 0 }}>
        textTrends
      </h1>
      <p style={{ color: 'var(--fg-muted)' }}>
        Rewrite in progress — a local-first corpus reading workbench.
      </p>
    </main>
  );
}
