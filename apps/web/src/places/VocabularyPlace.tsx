import { FrequencyTable } from '../components/CorpusDashboard.tsx';

/** Vocabulary owns ranked types and their distribution controls. */
export function VocabularyPlace() {
  return <FrequencyTable showHeading={false} />;
}
