{
  "name": "texttrends-monorepo",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@10.19.0",
  "devDependencies": {
    "@texttrends/epub": "workspace:*",
    "@texttrends/standard-ebooks": "workspace:*",
    "fflate": "0.8.3",
    "vitest": "^4.1.0"
  },
  "scripts": {
    "dev": "pnpm build:packages && pnpm --filter @texttrends/web dev",
    "dev:tailnet": "pnpm build:packages && bash scripts/dev-tailnet.sh",
    "build": "pnpm typecheck && pnpm --filter @texttrends/web build:bundle && pnpm check:bundle",
    "check:bundle": "node scripts/check-bundle.mjs",
    "build:packages": "pnpm --filter @texttrends/epub build && pnpm --filter @texttrends/standard-ebooks build",
    "test": "pnpm build:packages && node --test 'scripts/*.test.mjs' && vitest run",
    "typecheck": "pnpm build:packages && pnpm -r typecheck",
    "e2e": "pnpm --filter @texttrends/web e2e",
    "update:supplemental-corpus": "pnpm build:packages && node scripts/update-supplemental-corpus.mjs",
    "update:stoplist": "node scripts/update-stoplist.mjs",
    "update:se-catalog": "pnpm build:packages && node scripts/update-se-catalog.mjs",
    "update:austen-corpus": "pnpm build:packages && node scripts/update-austen-corpus.mjs",
    "update:sherlock-corpus": "pnpm build:packages && node scripts/update-sherlock-corpus.mjs",
    "update:demo-corpora": "pnpm build:packages && node scripts/update-demo-corpora.mjs"
  }
}
